# DPO Data Generation Requirements

# Core dependencies
torch>=2.0.0
transformers>=4.37.0
pillow>=9.0.0
numpy>=1.21.0

# Qwen-specific dependencies
qwen-vl-utils

# Optional dependencies for better performance
flash-attn>=2.0.0  # Optional: for flash attention support
accelerate>=0.20.0  # For better device management

# Development dependencies (optional)
pytest>=7.0.0
black>=22.0.0
flake8>=4.0.0
